MiSTerVision

Install on MiSTer

1. Exit MiSTerVision. Keep a copy of the installed binaries before upgrading.
2. Choose the progressive ZIP (normally 240p) or interlaced ZIP (normally 480i) for a new installation. Both include the same binaries and core. Extract the ZIP on your computer. Copy Scripts/MiSTerVision.sh to /media/fat/Scripts/ and the contents of mistervision/ to /media/fat/mistervision/ on the SD card.
3. For local Jellyfin discovery, leave connection settings absent and select a server when the app starts. For Plex discovery, open About, choose Connections, then choose Plex and link your account. For an explicit Jellyfin or Plex address, copy settings.example.json to settings.json in /media/fat/mistervision/ and set server.provider and server.url. Use provider "jellyfin" with port 8096 or "plex" with port 32400, unless your server uses another port.
4. Keep existing settings and saved state when upgrading. Existing jellyfin.conf remains supported when settings.json has no server section. See the configuration guide in the source bundle or online: https://github.com/trentnix/mistervision/blob/main/docs/GO_CONFIGURATION.md
5. Make the launcher and both binaries executable if the filesystem requires it.
6. Launch MiSTerVision from MiSTer's Scripts menu. Approve Jellyfin Quick Connect in a signed-in Jellyfin client, or enter the Plex code at plex.tv/link. For Plex Home, choose a viewer and enter their PIN if requested. Open About to switch viewers or connections later.

When moving from MiSTerFin CRT, exit the old application and back up its directory. Copy its settings, state, caches, optional InterlacedMenu.rbf, and custom assets into /media/fat/mistervision before installing this bundle. Update absolute paths in settings and remove the old Scripts entry after verification. Do not copy old binaries over the new pair. MiSTerFin CRT v1.0.x cannot install this renamed bundle through About.

The archive contains no active settings, credentials, saved state, or caches. Copying its files preserves those files in an existing installation. Stop the application before replacing either binary, and always replace the pair.

First installation is manual. Upgrading from v1.4.0 or earlier also requires one manual installation. The older About page shows the release notes without an Install action because the archive names changed. Subsequent compatible releases can be installed from About in a standard MiSTer installation. Review the release notes and select Install. The app restarts automatically after installation. Custom launchers without restart support require reopening the app after an update. Interrupted replacements recover at startup. Keep .update-pending if recovery is needed.

The default is progressive output, normally 240p on NTSC. The same binaries support optional 480i. Both packages include the interlaced Menu core. On first launch, the preset creates settings.json only when no current or legacy configuration exists. Existing installations keep their settings and display mode, whichever ZIP is copied. Follow docs/GO_DISPLAY.md in the source bundle, or the online display guide: https://github.com/trentnix/mistervision/blob/main/docs/GO_DISPLAY.md

Checksums and source

The release's SHA256SUMS checks the ZIP and source archive. On Linux, run sha256sum -c SHA256SUMS in the directory containing those downloads. The ZIP also contains checksums for its extracted files.

VERSION identifies the release. BUILD.txt records its revision, compiler metadata, and executable checksums. The source archive includes the project, MPlayer's upstream source, patches, build recipes, and the exact interlaced Menu source archive. Its third_party/Menu_MiSTer-source.tar.gz contains the complete upstream Quartus project. To rebuild MPlayer, extract the source bundle and run make native-player from its project directory. Docker still needs its pinned base image and compiler packages.

Application and component terms are in LICENSE, THIRD_PARTY.md, and licenses/. The source bundle contains the documentation referenced by those notices.
