misterfin
misterfin-arm
misterfin-preview
mplayer-arm
jellyfin.conf
/input.json
/music.json
/sounds.json
token.conf
device.conf
crash.log
debug.log
__pycache__/
*.pyc
.DS_Store
.claude/
local-preview.png
# Object files: `zig cc -c` writes these into the repo root when it is used
# directly for a per-file warning check (CFLAGS_ARM carries no -Wall, so that
# is worth doing), and they are trivially easy to sweep up with `git add -A`.
*.o
/build/

/display.json

/diagnostics.json
debug.log.1

/background.json
/ui.json

/settings.json

# Verified upstream source supplied by a release source bundle.
/docker/MPlayer-source.tar.xz

# Local release announcement images and drafts.
announcements/
